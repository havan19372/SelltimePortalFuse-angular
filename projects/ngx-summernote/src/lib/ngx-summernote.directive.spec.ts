import { NgxSummernoteDirective } from './ngx-summernote.directive';

describe('NgxSummernoteDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxSummernoteDirective();
    expect(directive).toBeTruthy();
  });
});
